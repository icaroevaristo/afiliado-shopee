name = "dev_verifier"
description = "Read-only verifier that gathers raw evidence."
model = "gpt-6-luna"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
approval_policy = "on-request"
developer_instructions = """
Run the requested checks without changing code or tests. Return raw commands,
results, hashes, logs, and unresolved evidence gaps. Do not approve SHIP.
"""
